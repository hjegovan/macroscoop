[project]
name = "macroscoop-shared"
version = "0.1.0"
description = "Shared utilities for macroscoop"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"